<%= form_for(@timeset) do |f| %>
  <% if @timeset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timeset.errors.count, "error") %> prohibited this timeset from being saved:</h2>

      <ul>
      <% @timeset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
