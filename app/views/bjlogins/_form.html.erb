<%= form_for(@bjlogin) do |f| %>
  <% if @bjlogin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bjlogin.errors.count, "error") %> prohibited this bjlogin from being saved:</h2>

      <ul>
      <% @bjlogin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
